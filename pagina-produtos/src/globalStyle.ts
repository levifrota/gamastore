import { createGlobalStyle } from "styled-components";

export default createGlobalStyle`
  *{
    padding: 0;
    margin: 0;
    font-family: 'Asket Narrow Light';
    font-style: normal;
    font-weight: normal;
    src: local('Asket Narrow Light'), url('Asket Narrow Light.woff') format('woff');
  }
`;
